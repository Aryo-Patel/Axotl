import React, {useEffect, Fragment} from 'react'
import {connect} from 'react-redux'
import PropTypes from 'prop-types'

const Login = {sendLogin} => {
    return (
        <Fragment>
            
        </Fragment>
    )
}

Login.propTypes = {

}

export default connect(null, {sendLogin})(Login);
